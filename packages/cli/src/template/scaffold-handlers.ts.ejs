// SPDX-License-Identifier: Apache-2.0

// Auto-generated
<% props.abis.forEach(function (abi) { %>
import {<% abi.handlers.forEach(function (handler) { %><%= handler.argType %>,<% })%>} from "../types/abi-interfaces/<%= abi.name %>";
<% })%>
import assert from "assert";
<% props.abis.forEach(function (abi) { %><% abi.handlers.forEach(function (handler) { %>
export async function <%=handler.name %>_<%=abi.name %> (<%=handler.argName%>: <%=handler.argType %> ): Promise<void> {
// Place your code logic here
}
<% })%><% })%>