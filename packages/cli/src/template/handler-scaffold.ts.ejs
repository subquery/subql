// SPDX-License-Identifier: Apache-2.0

// Auto-generated

<% props.abis.forEach(function (abi) { %>
import {<% props.handlers.forEach(function (handler) { %><%= handler.argType %>,<% })%>} from "../types/abi-interfaces/<%= abi%>";
<% })%>
import assert from "assert";
<% props.handlers.forEach(function (handler) { %>
<% props.abis.forEach(function (abi) { %>
export async function <%=handler.name %>_<%=abi %> (data: <%=handler.argType %> ): Promise<void> {
// Place your code logic here
}
<% })%><% })%>