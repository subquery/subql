{
  "name": "@subql/types",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && tsc -b"
  },
  "author": "Ian He",
  "main": "dist/index.js",
  "license": "Apache-2.0",
  "files": [
    "src/global.d.ts",
    "/dist"
  ],
  "peerDependencies": {
    "@polkadot/api": "^3.0.1"
  },
  "devDependencies": {
    "@polkadot/api": "^3.0.1",
    "@types/app-module-path": "^2.2.0"
  }
}
